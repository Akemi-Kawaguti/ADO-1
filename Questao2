/*Escreva um programa em Java que receba um número inteiro positivo N do usuário e 
calcule a soma de todos os números pares de 1 até N (inclusive, se N for par).*/

/*Use estrutura de repetição (for, while, ou do-while).
Valide que o número seja positivo.
Use apenas conceitos básicos (nada de bibliotecas avançadas).*/

import java.util.Scanner;

public class Questao2{
    public static void main(String [] args){

        int numero, acumulador=0;
        Scanner sc = new Scanner (System.in);

/* Caso fosse usar :Recursividade
    public static int npositivo(){
    System.out.println("Digite um número inteiro");
    int numero= sc.nextInt();

      if (numero <=0){
      System.out.println("Digite um número inteiro");
      return npositivo(); //repete processo até usuário acertar
      } return numero;} //finaliza a recursão e entrega o resultado */

//Para garantir que usuário digite número positivo

            do{
                System.out.println("Digite um número inteiro");
                numero= sc.nextInt();
            } while (numero <=0);

//

                for (int i = 2; i <= numero ; i +=2){
                    acumulador += i;
                }


        System.out.println("A soma dos números pares de 1 até "+numero+" é: "+acumulador);


        sc.close();
    }
}
